# 地震哨兵 (Dianguard) · Earthquake Sentinel

> **多源融合地震预警引擎** —— 将一台旧手机变成守护家人的秒级预警哨兵。

🌐 **官网**：[lokeily.github.io/dianguard-site](https://lokeily.github.io/dianguard-site/)　|　📦 **最新版**：[v1.3.2](https://github.com/Lokeily/Earthquake-Sentinel/releases)

---

## 项目简介

Dianguard 是一款面向中国大陆的**个人级地震预警 Android 应用**，目标场景是"插上旧手机当私人预警哨兵"。软件实时订阅国家级地震预警（EEW）数据，在破坏性地震波（S 波）到达前数秒至数十秒，以**全屏锁屏告警 + 最大音量语音 + 震动**组合强制唤醒用户，争取宝贵的逃生窗口。

- **minSdk 21 / targetSdk 34**，全 Kotlin 实现，适配 Android 5.0+ 旧机型长期常驻
- **零第三方 SDK**：仅 AndroidX + OkHttp + org.json，攻击面最小化，无任何数据上报
- **98 项单元测试**全通过，覆盖解析 / 烈度衰减 / 多源融合 / 震级校准 / 跨源去重
- **五路实时源 + 三路备用源**，全链路 SPKI 证书固定

---

## 核心架构

Dianguard 采用"**多路实时源 + 融合决策 + 分区烈度衰减 + 渐进式响应**"四层架构，底层物理模型基于 GB 18306-2015《中国地震动参数区划图》，上层决策由自研多源融合引擎驱动。

```
实时数据源（五路并行，任一可达即可预警）
  ├─ 中国地震台网 CENC ─┐
  ├─ 四川省地震局  SC  ─┤ WebSocket 秒级推送
  ├─ 重庆市地震局  CQ  ─┤ （Wolfx 镜像，ws-api.wolfx.jp）
  ├─ 台湾地震测报  CWA ─┘
  └─ ICL 减灾所官方 ───── HTTP 3s 轮询（mobile-new.chinaeew.cn，域名独立）
        │
        ▼
  解析层：parseEew / parseExternalEew / parseIclEew / parsePodrisEew
        │
        ▼
  多源融合决策引擎 ── 交叉验证 · 动态融合窗 · 抗异常值 · 震级校准
        │
        ▼
  GB 18306-2015 分区烈度衰减 ── 6 分区 + 边界防抖 + 0.5σ 安全边际
        │
        ▼
  AlertActivity ── 全屏锁屏 · 四级真实语音 · 倒计时 · 烈度分级
        │
        ▼
  备用探测源（主链路全断时激活）── USGS + Wolfx HTTP + EMSC，30s 轮询
```

---

## 数据源拓扑

### 实时源（触发全屏告警）

| 源 | 机构 | 协议 | 鉴权 | 说明 |
|------|------|------|------|------|
| **CENC** | 中国地震台网 | WebSocket | 免 | 全国主源 |
| **SC** | 四川省地震局 | WebSocket | 免 | 西南地区互证 |
| **CQ** | 重庆市地震局 | WebSocket | 免 | 西南地区互证 |
| **CWA** | 中国台湾地震测报中心 | WebSocket | 免 | 覆盖台湾海峡震源，补足东南沿海离岸地震的延迟与欠报 |
| **ICL** | 成都高新减灾研究所 | HTTP 3s 轮询 | 免 | 中国地震预警网，**域名完全独立** |
| BeeCLD | 用户自注册（可选） | WebSocket | 用户自备 Key | App **不内置密钥**，需用户自行注册填写 |

> 前四路虽分属**三家不同机构**（可互为佐证），但均经 Wolfx 镜像分发、**共用 `ws-api.wolfx.jp` 单一域名**。
> 该网关故障时四路同时不可用，此时 **ICL 官方源为唯一独立实时链路**，预警不中断。
> 引入第二家互不包含的中转商已列入后续计划。

### 备用探测源（震后速报，**不触发**全屏告警）

| 源 | 协议 | 说明 |
|------|------|------|
| USGS FDSN | HTTP 30s | 真正的非 Wolfx 独立源，自带 2 小时长周期去重 |
| Wolfx HTTP | HTTP 30s | 主链路 WS 断开时的同源兜底 |
| EMSC FDSN | HTTP 30s | 独立第三源 |

> 备用源属"震后速报"，从发震到进入目录通常已数分钟，**预警时效已过**，故仅用于历史记录与状态展示。

---

## 核心算法

### 多源融合决策引擎

融合引擎在**动态融合窗口**内收集多源报文，通过**多源交叉验证 + 抗异常值融合**降低单源误报风险，并具备震级系统性偏差校准、坐标防抖、按事件版本隔离、超时强制输出等工程保障。

主要能力：

| 能力 | 说明 |
|------|------|
| 抗异常值融合 | 单源报错震级不会把融合结果拖偏 |
| 动态融合窗口 | 多源活跃时自动缩短窗口抢预警时间，单源时保守等待第二源 |
| 震级校准 | 内置离线预训练画像，按「源 × 震级区间」修正各源系统性偏差，修正量钳制 ±0.5 级 |
| 动态趋势积分 | 按各源相对共识的偏移趋势动态调权 |
| 坐标质心选优 | 取权重最高源的原始坐标，**严禁加权平均**（平均会产生地理上不存在的震中） |
| 坐标死区 | 偏移小于阈值时沿用旧坐标，防止高频震荡 |
| 版本隔离 | 按事件独立递增版本号，修正幅度过大时强制推送更新 |
| 状态 TTL 淘汰 | 事件级状态表 30 分钟无活动即淘汰，保证常驻运行下内存有界 |

> ℹ️ 本开源仓库包含融合引擎的**兼容精简实现**（加权平均 + 坐标选优 + 去重）；完整版引擎及其校准模块为**闭源专有**（详见文末《许可声明》），不随本仓库分发。上表描述的是发布版 APK 所搭载的完整版能力。

### GB 18306-2015 — 分区烈度衰减模型

放弃全国单一公式，改用第五代地震区划图的六分区短轴衰减模型（含震中坐标自动分区选择）：

```
I = A + B·M + C·lg(R + R₀) + 0.5σ
```

| 分区 | 覆盖范围 | 短轴系数 (A, B, C, R₀) | σ |
|------|---------|----------------------|-----|
| 川滇西南 | 云南全境+川西 | 2.941, 1.363, -1.494, 7 | 0.67 |
| 四川盆地 | 成都/重庆周边 | 3.456, 1.280, -1.608, 7 | 0.61 |
| 青藏区 | 青藏高原+甘南 | 3.368, 1.275, -3.312, 9 | 0.66 |
| 新疆区 | 天山南北 | 3.611, 1.435, -3.848, 13 | 0.59 |
| 东部强震区 | 华北+东北 | 3.659, 1.363, -3.541, 13 | 0.58 |
| 中强地震区 | 华南+华中 | 3.944, 1.071, -2.845, 7 | 0.52 |

**关键特性**：
- 云南深源地震区（depth > 60km）：启用郁曙君 1993 特殊公式
- 边界防抖：震中距分区边界 < 15km 时双区并行计算取 MAX
- 安全边际：触发阈值采用 +0.5σ 偏上分位数，宁可高报不低报
- 倒计时修正：S 波到达时间使用**空间震源距**（√(震中距² + 深度²)），深源地震不倒计时假警报
- 无效值拦截：M < 3.0 或 R < 0 直接返回 -1，跳过触发

### 告警触发与响应

```
本地预估烈度 ≥ 阈值(3°/2°/4°) ──→ 融合引擎决策 ──→ 全屏锁屏弹出
        │
        ▼
  倒计时弹窗等级（按【用户所在地预估烈度】，中国地震局官方标准）：
    Ⅰ级红≥7°（灾害性）· Ⅱ级橙5-6°（灾害性）· Ⅲ级黄3-4°（告知性）· Ⅳ级蓝<3°（告知性）
        │
        ▼
  三段式真实语音（大陆预警录音，非 TTS）：
    短语循环 → 逐秒倒计时 → 警报循环
        │
        ▼
  归零后显示避险指引：「趴下、掩护、抓牢 · 远离玻璃窗与重物」
```

> **两套分级标准**（对齐王暾团队"地震预警"App 设计）：**历史列表颜色 = 震级**（红≥6.0 / 橙5.0-5.9 / 黄3.0-4.9 / 蓝<3.0，代表地震本身有多强）；**倒计时弹窗颜色 = 烈度**（红≥7° / 橙5-6° / 黄3-4° / 蓝<3°，代表地震波传到你家时该怎么避险）。

---

## 界面与交互

- **iOS 风格视觉语言**：卡片式布局、系统级配色语义、统一圆角与留白
- **三 Tab 结构**：首页（监听状态 / 数据源 / 自检）· 历史（速报列表）· 设置
- **统一转场动效**：页面淡入 + 轻微缩放、Tab 淡入淡出、更多设置右侧滑入、历史列表逐项级联入场
- **数据源状态可视化**：绿色「已连接」/ 橙色「源异常·<HTTP 码>」/ 灰色「连接中…」，
  服务端故障与本地网络问题一眼可辨，源恢复后自动转回绿色

> ⚠️ **告警页刻意不加任何动画**。全屏告警是生命安全场景，任何入场动效都是对逃生时间的浪费——保持瞬时弹出。

---

## 安全架构

### 证书固定（Certificate Pinning）

所有预警/历史数据链路均实施 **SPKI 证书固定**（leaf + 中间 CA 双 pin），防中间人注入虚假地震预警：

| 域名 | 用途 | 固定方式 |
|------|------|---------|
| `ws-api.wolfx.jp` | 实时 EEW（CENC/SC/CQ/CWA） | leaf + Google Trust Services WE1 |
| `api.wolfx.jp` | 历史速报主源 | leaf + Google Trust Services WE1 |
| `mobile-new.chinaeew.cn` | ICL 官方轮询源 | leaf + WoTrus RSA DV SSL CA 2 |
| `earthquake.usgs.gov` | 备用源 | leaf + DigiCert Global G2 |
| `www.seismicportal.eu` | 备用源 | leaf + Let's Encrypt YR2 |
| `api.bigdatacloud.net` | 逆地理编码 | leaf + Thawte TLS RSA CA G1 |

> 即使攻击者控制网络层，也无法用伪 CA 证书冒充数据源。leaf 轮换由中间 CA pin 兜底，不中断预警链路。
> 降级逃生口仅在**证书校验类异常**（`SSLPeerUnverifiedException` / `CertificateException`）时触发，
> 普通 HTTP 错误（如 503）**不会**导致固定被绕过。

### 防伪造 / 防重放

- **传输层**：证书固定防中间人（上表）
- **应用层**：多源融合交叉验证——单源伪造报文会被融合引擎的抗异常值机制抑制，双源以上一致才触发高等级告警
- **去重层**：10 分钟跨源去重窗口（quakeKey 量化归并），同事件多报不重复弹窗

### 组件暴露面

| 项 | 配置 |
|------|------|
| 导出组件 | 仅 Launcher `MainActivity`；其余 Activity / Service / Receiver 全部 `exported=false` |
| FileProvider | `exported=false` + `grantUriPermissions`，路径白名单受控 |
| 数据备份 | `allowBackup=false`，防止本地配置被 adb 备份提取 |
| 明文流量 | `usesCleartextTraffic=false`，全链路 HTTPS / WSS |

### 密钥管理

- 签名密钥 `*.jks` / `local.properties` 均已 `.gitignore` 忽略，**从未进入版本库**
- 签名密码支持环境变量注入（`DIANGUARD_STORE_PASSWORD` 等），CI / 发布时无需落盘明文
- 崩溃日志 `crash.txt` 坐标自动脱敏
- App **不内置任何第三方 API 密钥**；可选源的 Key 由用户自行注册填写，仅存本机

---

## 隐私

- 位置坐标**仅保存在本机** SharedPreferences，除逆地理编码（获取地名）外不向任何服务器发送
- 无埋点、无统计 SDK、无崩溃上报服务、无广告
- 历史记录以 JSON 存本地，不引入数据库，卸载即清除

---

## 权限说明

| 权限 | 用途 |
|------|------|
| INTERNET / ACCESS_NETWORK_STATE | 数据源订阅 |
| FOREGROUND_SERVICE / _DATA_SYNC | 常驻后台监听 |
| WAKE_LOCK | 后台保活（4h 无条件续期） |
| POST_NOTIFICATIONS | 预警推送 |
| ACCESS_FINE / COARSE_LOCATION | 计算震中距（仅本机保存） |
| SYSTEM_ALERT_WINDOW | 锁屏强制弹出告警 |
| VIBRATE | 告警震动 |
| REQUEST_INSTALL_PACKAGES | 应用内更新调起系统安装器 |
| RECEIVE_BOOT_COMPLETED | 开机自启恢复监听 |
| REQUEST_IGNORE_BATTERY_OPTIMIZATIONS | 防后台被杀引导 |

**全部权限均有明确功能对应，无冗余申请。**

---

## 预警知识库（官方标准依据）

### 预警五要素

地震预警利用**电波比地震波快**的原理，在破坏性地震波（S 波）到达前向用户推送预警。预警信息包含五要素：

| 要素 | 说明 |
|------|------|
| 发震时刻 | 地震发生的时刻（由预警系统自动测定） |
| 震中位置 | 震源在地表的投影位置 |
| 预警震级 | 系统自动分析产生的震级，会随监测信息增加而更新（应用内展示为「X.X级地震」） |
| 预估烈度 | 地震对**您所在区域**的影响和破坏程度（应用内按此分级配色） |
| 预警时间 | 破坏性地震波即将到达的倒计时（应用内全屏倒计时） |

> 一次地震只有一个震级，但在不同地区烈度不同（如同炸弹的炸药量与各处破坏程度的关系）。示例：常熟-太仓 5.1 级地震，太仓沙溪镇烈度 6 度、苏州 4 度、无锡 3 度——震级全境相同，烈度因地而异。

### 震级分类（GB/T 17742-2020 依据）

| 分类 | 震级范围 | 特征 |
|------|---------|------|
| 弱震 | < 3 级 | 震源不浅时一般不易察觉 |
| 有感地震 | 3 – 4.5 级 | 人能感觉到，一般不造成破坏 |
| 中强震 | 4.5 – 6 级 | 可造成破坏（轻重与震源深度、震中距有关） |
| 强震 | ≥ 6 级 | 破坏性强；≥ 8 级又称巨大地震 |

> 震级每相差 1.0 级，能量相差约 30 倍；相差 2.0 级，能量相差约 900 倍。

### 地震烈度 12 级（《中国地震烈度表》GB/T 17742-2020 节选）

| 烈度 | 人的感觉 / 破坏情况 |
|------|---------------------|
| Ⅰ–Ⅱ（1–2）| 无感；室内个别静止中的人有感觉 |
| Ⅲ–Ⅳ（3–4）| 室内多数人有感；悬挂物明显摆动，器皿作响 |
| Ⅴ（5）| 多数人睡梦中惊醒；悬挂物大幅度晃动，水从盛满容器溢出 |
| Ⅵ（6）| 多数人站立不稳、惊逃户外；少数轻家具移动，河岸软土裂缝 |
| Ⅶ（7）| 物品从架子掉落、家具倾倒；河岸塌方，地裂缝较多 |
| Ⅷ（8）| 多数人摇晃颠簸、行走困难；除重家具外多数物品倾倒 |
| Ⅸ（9）| 行动的人摔倒；供水管道广泛破坏，大范围停水，滑坡塌方常见 |
| Ⅹ（10）| 骑自行车的人摔倒；山崩和地震断裂出现 |
| Ⅺ–Ⅻ（11–12）| 地震断裂延续很大，大量山崩滑坡，地面剧烈变化、山河改观 |

> 应用内预警等级即按此标准映射：**红 ≥ 7°（灾害性）｜橙 5–6°（灾害性）｜黄 3–4°（告知性）｜蓝 < 3°（告知性）**。高楼层有放大效应，震感可能更明显。

---

## 构建

```bash
# 环境：Android SDK 34 / JDK 17 / Gradle 8.9
./gradlew assembleRelease
```

产物：`app/build/outputs/apk/release/app-release.apk`

> 签名凭据：优先读取环境变量 `DIANGUARD_KEYSTORE_PATH` / `DIANGUARD_STORE_PASSWORD` / `DIANGUARD_KEY_ALIAS` / `DIANGUARD_KEY_PASSWORD`，回退 `local.properties`。

---

## 测试

```bash
./gradlew testDebugUnitTest   # 98 个用例
./gradlew lintDebug           # 静态分析质量门禁
```

| 测试类 | 数量 | 覆盖 |
|--------|------|------|
| CoreLogicTest | 66 | 解析 / 烈度衰减 / 去重 / 分级 |
| FusionCalibrationTest | 27 | 分桶查表 / 钳制 / 分轨 / 持久化 / 学习开关 |
| EewFusionTest | 5 | 动态窗口 / 双源立即融合 / 修正融合 / 稳定性 |

> 开源仓库（精简引擎）为 66 项；发布版工程（完整引擎）为 98 项。

---

## 版本历史

- **v1.3.2** — 融合引擎 v2.4 完整版；修复事件状态表内存泄漏；告警页文案与冗余信息修正；全局动效统一（详见 [CHANGELOG-v1.3.2.md](CHANGELOG-v1.3.2.md)）
- **v1.3.1** — 融合引擎闭源化拆分；双许可声明
- **v1.3.0** — 单点风险治理：移除内置密钥源；ICL 官方源独立接入
- **v1.2.0** — 分区烈度衰减模型；证书固定

---

## 参考文献

- 汪素云, 俞言祥, 高阿甲, 等. 中国分区地震动衰减关系的确定[J]. 中国地震, 2000, 16(2): 99-106.
- 雷建成, 高孟潭, 俞言祥. 四川及邻区地震动衰减关系[J]. 地震学报, 2007, 29(5): 500-511.
- 郁曙君. 川滇地区地震烈度衰减关系[J]. 地震工程与工程振动, 1993, 13(1): 58-66.
- GB 18306-2015《中国地震动参数区划图》
- GB/T 17742-2020《中国地震烈度表》
- 肖亮, 俞言祥. 新一代地震区划图地震动参数衰减关系的建立与特点分析[J]. 中国地震局, 2011.

---

## 许可声明

本项目采用**双许可**模式：

- **开源部分**：除下述闭源组件外的全部源码（UI、数据源解析、烈度衰减模型、告警链路、历史记录、证书固定、构建配置等）遵循 **Apache License 2.0** 开源协议，可自由使用、修改与分发（保留版权声明）。
- **闭源部分**：**多源融合决策引擎及其校准模块**（`EewFusion.kt` 完整版、`FusionCalibration.kt` 等，以及随版本迭代演进的对应实现）为**闭源专有**，**不随本仓库分发**，其完整实现仅由作者维护。本仓库中同名文件的**开源精简兼容实现**仅保留对外接口（`ingest` / `forceFlush` / `FusionResult`），内部算法为简化加权融合，**不构成**闭源引擎的授权或替代。

> 使用本仓库代码即表示同意上述许可划分。闭源组件的任何商业使用、二次开发集成需另行联系作者授权。

---

## 免责声明

本项目仅供学习和研究使用。地震预警涉及生命安全，请以官方预警渠道为准。使用本应用即表示已阅读并同意《免责声明与用户协议》。
