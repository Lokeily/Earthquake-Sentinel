# 地震哨兵 v1.3.2 更新细则

**发布日期**：2026-08-06　|　**versionCode**：25　|　**上一版本**：v1.3.1 (versionCode 24)

> 本次更新的主线是**融合引擎升级到 v2.4 完整版**，配套修复一处长期潜伏的内存泄漏，
> 并对全应用交互动效与告警页文案做了系统性打磨。**无破坏性变更，可直接覆盖安装。**

---

## 一、融合引擎 v2.4（核心）

### 1.1 引擎能力全量启用

v1.3.1 的发布包使用的是**兼容精简版**融合逻辑（加权平均 + 坐标选优 + 去重）。
v1.3.2 起，发布包搭载**完整版多源融合决策引擎**，新增以下能力：

| 能力 | 说明 |
|------|------|
| **加权中位数融合** | 取代简单加权平均，从根本上抗单源异常值污染——某一源报错震级时不再把融合结果拖偏 |
| **动态融合窗口** | ≥2 源近期活跃时窗口由 1500ms 缩短至 **1000ms**，为多源场景抢回约 0.5s 预警时间；单源时保持 1500ms 保守等待第二源 |
| **动态趋势积分** | 追踪各源震级相对共识的偏移趋势，连续偏高/偏低即动态调权（封顶 +0.20，连续 3 次不变归零） |
| **校准-权重协同** | 源权重 = 可靠性权重 × 稳定性因子（稳定性 = 1−σ，钳制 0.3~1.0），历史波动大的源自动降权 |
| **初报/终报分轨** | 初报与终报分别建模——初报偏差才是预警第一时间真正关心的，分开建模避免混合污染 |
| **版本锁机制** | 修正幅度 > ±0.4 级时强制推送更新，防止用户停留在明显过时的判断上 |

### 1.2 震级校准模块（FusionCalibration）

新增内置**预训练分桶画像**，开机即用，无需等待样本积累：

- **训练依据**：以国际权威地震目录（ISC / USGS FDSN）的中国区正式解作为真值基准，
  离线统计各数据源相对真值的系统性偏差，按「源 × 震级区间」分桶建模
- **已知偏差方向**：速报目录类源倾向**低估**，EEW 预警初报类源倾向**偏高**
  （初报基于台站首波快速估算，本身即偏保守取大）——引擎按源分别反向修正
- **修正量钳制 ±0.5 级**——防止校准本身成为新的风险源
- **坐标不修正**：实测各源震中与真值偏差已在可接受范围内，无需引入额外误差

> **在线学习默认关闭**。当前用户规模下在线样本趋近于零，收益不足以抵消写盘与内存开销。
> 引擎进化改为在开发机本地用训练管道完成，随版本更新内置。学习代码完整保留，
> 未来规模上来后开关即可恢复。

### 1.3 🐛 内存泄漏修复（重要）

**问题**：融合引擎中 4 张按事件累积的状态表（`lastOutput`、`eventVersion`、
`eventTrendBonus`、`eventTrendStreak`）此前**只增不减**——作为常驻前台服务，
其占用随累计处理的地震次数单调增长，长周期运行（数月不重启）下会持续吃掉内存。

**修复**：引入事件级 `lastTouch` 时间戳，超过 **30 分钟**无活动的事件状态随
`cleanupStale` 周期一并淘汰，使内存占用在长周期运行下**有界**。趋势表的
Key 为 `quakeKey|sourceId`，按前缀批量清除该事件下所有源的条目。

> 这是本次更新中**唯一影响长期稳定性的实质缺陷修复**，建议所有用户升级。

### 1.4 测试覆盖

单元测试由 **66 项**提升至 **98 项**，全部通过：

| 测试类 | 数量 | 覆盖 |
|--------|------|------|
| CoreLogicTest | 66 | 解析 / 烈度衰减 / 去重 / 分级 |
| FusionCalibrationTest | 27 | 分桶查表 / 钳制 / 分轨 / 持久化 / 学习开关 |
| EewFusionTest | 5 | 动态窗口 / 双源立即融合 / 修正融合 / 稳定性 |

---

## 二、告警页修正

### 2.1 黄色等级文案

预估烈度 3–4° 的告警标题由「**强有感**」修正为「**强有感地震**」——
与红/橙/蓝三级的措辞体例保持一致，避免出现单独一个形容词的割裂感。

### 2.2 移除重复的震中距信息

告警页此前存在**两处**震中距展示：中部信息行（"震中距约 90km · 预估烈度 2.9度 · 深度 10km"）
与底部脚注行。本次移除中部冗余行，**底部脚注作为唯一距离信息源**保留完整内容
（第 X 报 · 来源 · 距您 XX km · 时间）。

「我已安全 / 停止警报」按钮区域**未做任何改动**。

---

## 三、界面与动效

本次对全应用交互动效做了统一设计，取代此前生硬的默认切换：

| 位置 | 动效 |
|------|------|
| **页面转场** | 淡入 + 轻微缩放（0.96→1.0，280ms，fast_out_slow_in），退出 220ms |
| **底部 Tab 切换** | Fragment 淡入淡出（200ms / 150ms） |
| **更多设置进入** | 右侧滑入 + 淡入（300ms），返回时反向滑出 |
| **历史列表** | 逐项级联入场（上移 16dp + 淡入，间隔 0.05） |
| **历史功能卡** | 淡入（280ms） |
| **设置页底部浮层** | 上浮 + 缩放入场（260ms）→ 停留 2.2s → 下沉淡出（360ms） |
| **测试报警配置面板** | 淡入 220ms / 淡出 180ms，带动画取消保护 |

> ⚠️ **告警页刻意不加任何动画**。全屏告警是生命安全场景，任何入场动效都是
> 对逃生时间的浪费——`Theme.Dianguard.Alert` 未继承窗口动画样式，保持瞬时弹出。

### 3.1 返回控件统一

免责声明、使用引导、测试报警三个页面的返回控件，由文字「‹ 返回」统一为
**图标按钮**（`ic_chevron_left`，32dp，带点击水波纹），与「更多设置」页保持一致。

### 3.2 移除冗余说明文字

移除设置页「测试报警 / 更多设置」卡片下方的说明小字——按钮语义已经足够清楚，
多余的解释反而增加视觉噪音。

---

## 四、连接状态显示

数据源状态新增**服务端异常**的独立展示：源返回 HTTP 错误（如 503）时，
状态行显示**橙色**「源异常·503」，与灰色「连接中…」区分开——
让用户一眼分清「是对方服务器挂了」还是「我这边网络在连」。

源恢复正常后，重连成功会清除该标记，状态行**自动转回绿色「已连接」**，无需手动干预。

---

## 五、安全与合规核查（发布前审计）

本次发布前完成了一轮完整审计，结果如下：

| 审计项 | 结果 |
|--------|------|
| 密钥泄露（git 历史 / 当前跟踪） | ✅ 无 `.jks` / `local.properties` / keystore 泄露 |
| 硬编码令牌扫描 | ✅ 无任何硬编码 API Key / Token / 密码 |
| 组件导出面 | ✅ 仅 Launcher `MainActivity` 导出，其余 Activity / Service / Receiver 均 `exported=false` |
| FileProvider | ✅ `exported=false` + `grantUriPermissions`，路径白名单受控 |
| 数据备份 | ✅ `allowBackup=false`（防止本地配置被 adb 备份提取） |
| 明文流量 | ✅ `usesCleartextTraffic=false`，全链路 HTTPS/WSS |
| 权限清单 | ✅ 11 项权限均有实际使用点，无冗余申请 |
| API 21–22 兼容 | ✅ `canDrawOverlays` 三处调用点均有 `Build.VERSION >= M` 守卫 |
| R8 资源保留 | ✅ `keep.xml` 覆盖全部动态引用的 raw 音频资源 |
| 单元测试 | ✅ 98/98 通过 |

---

## 六、已知限制

1. **Wolfx 单点依赖**：CENC / 四川局 / 重庆局 / CWA 四条 WebSocket 均托管于
   `ws-api.wolfx.jp` 单一域名，该网关故障时四源同时不可用（近期 503 即属此情形）。
   此时 **ICL 官方源（`mobile-new.chinaeew.cn`）为唯一独立实时链路**，仍可正常预警。
   引入第二家互不包含的中转商已列入后续版本计划。

2. **在线自学习关闭**：如前述为主动设计决策，非缺陷。

---

## 升级建议

**建议所有用户升级。** 核心理由是 §1.3 的内存泄漏修复——它直接影响长期常驻场景
（把旧手机插电当哨兵）下的稳定性，正是本应用的主要使用方式。
